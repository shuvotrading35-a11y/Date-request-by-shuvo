/* ============================================
   CONFIG.JS — Backend API Base URL
   Date Request Platform
   ============================================ */

'use strict';

// Backend deployed on Railway — update this if the backend URL changes
window.API_ORIGIN = 'https://date-request-by-shuvo-production.up.railway.app';
window.API_BASE_URL = `${window.API_ORIGIN}/api/v1`;
